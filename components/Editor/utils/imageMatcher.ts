// import { Quill } from 'react-quill'

// const Delta = Quill.import('delta')

const imageMatcher = (upload: any) => (node: Node, delta: any) => {
  // const newDelta = new Delta()
  // newDelta.insert({ break: '' })
  return delta
}

export default imageMatcher
